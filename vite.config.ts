import { defineConfig } from 'vite';

export default defineConfig({
    base: '/LF-SE-Minor-Project_Dungeon-Dread/',
    build: {
        rollupOptions: {
        },
    },
    server:{
        open:true,
    }
});
